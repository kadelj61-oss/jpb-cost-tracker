# The one password used to log into the app. Set this in Vercel's
# Project Settings > Environment Variables — do not put the real value here.
APP_PASSWORD=

# These are added automatically when you attach a Postgres database
# to the project in Vercel (Storage > Create Database). You don't
# need to set them by hand.
# POSTGRES_URL=
